# Architecture notes

This directory holds the "how it actually works" write-ups for the systems described at a decision level in `docs/adr/`:

- The Glass Layer and coordinate transforms (ADR 0001)
- The undo/redo command stack (ADR 0003)
- The save/export pipeline (Blueprint v2, Section 6.4)

These are written as each system is implemented (Phase 2 onward per `docs/blueprint/pdf_pro_development_blueprint_v2.md`, Section 10), not speculatively ahead of the code.
